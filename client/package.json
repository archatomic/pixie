{
  "name": "pixie-client",
  "version": "0.0.1",
  "license": "MIT",
  "dependencies": {
    "parcel": "^2.7.0"
  },
  "scripts": {
    "start": "parcel ./index.html --open",
    "build": "parcel build ./index.html --dist-dir ../dist"
  }
}
